<configuration>

    <appender name="custom" class="m3.users.config.logback.KafkaAppender">

    </appender>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %clr(%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX}){faint} %clr(%5p) %clr(78988){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n%wEx
            </pattern>
        </encoder>
    </appender>

    <root level="debug">
        <appender-ref ref="custom" />
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>